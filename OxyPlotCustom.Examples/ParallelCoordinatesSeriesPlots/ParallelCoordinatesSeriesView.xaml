<UserControl
    x:Class="OxyPlotCustom.Examples.ParallelCoordinatesSeriesPlots.ParallelCoordinatesSeriesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OxyPlotCustom.Examples.ParallelCoordinatesSeriesPlots"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oxy="http://oxyplot.org/wpf"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0">
                <TextBlock
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Text="Enable Interaction" />
            </Border>
            <Border Grid.Column="1">
                <ToggleButton
                    x:Name="ToggleButton"
                    Width="80"
                    Height="30"
                    Margin="5"
                    IsChecked="False" />
            </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
            <oxy:PlotView x:Name="ParallelCoordinatesSeriesPlot" Model="{Binding PlotModel}" />
        </Grid>
    </Grid>
</UserControl>
